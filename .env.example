# Bridge API Configuration
PORT=3727
NODE_ENV=development

# LLM Provider (for quiz generation and evaluation)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Mock mode — set to "true" to use hardcoded responses instead of live LLM calls
BRIDGE_MOCK_MODE=true
