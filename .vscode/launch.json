{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Bridge Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "${workspaceFolder}",
        "--extensionDevelopmentPath=${workspaceFolder}/apps/extension"
      ],
      "outFiles": ["${workspaceFolder}/apps/extension/dist/**/*.js"],
      "preLaunchTask": "build-extension"
    }
  ]
}
