/**
 * @bridge/ui-kit
 *
 * Shared UI primitives for Bridge webviews.
 * This package will contain reusable HTML/CSS component generators
 * for the VS Code extension sidebar and panels.
 */

// Placeholder — will be populated with webview UI helpers
export {};
